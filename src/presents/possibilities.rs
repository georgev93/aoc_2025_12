use super::*;

pub struct PresentPossibilities {
    possibilities: Vec<Present>,
    free_space: usize,
}

impl PresentPossibilities {
    // pub fn new(input: &str) -> {
    //     let possibilities = Vec::with_capacity(8);
    //
    // }
}
